<!--	Build the distribution archive for LSC	-->

<!--	TODO:
			- Add bin directory (startup script, Nagios script?
			- Add cron and logrotate files
-->

<assembly xmlns="http://maven.apache.org/plugins/maven-assembly-plugin/assembly/1.1.0"
  xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
  xsi:schemaLocation="http://maven.apache.org/plugins/maven-assembly-plugin/assembly/1.1.0 http://maven.apache.org/xsd/assembly-1.1.0.xsd">
	<id>dist</id>
	<formats>
		<format>zip</format>
		<format>tar.gz</format>
	</formats>
	<baseDirectory>lsc-${project.version}</baseDirectory>

	<fileSets>
		<!-- Include text files describing the project -->
		<fileSet>
			<includes>
				<include>${project.basedir}/README*</include>
				<include>${project.basedir}/LICENSE*</include>
				<include>${project.basedir}/CHANGES*</include>
			</includes>
		</fileSet>
			
		<!-- Include the bin and etc directories -->
		<fileSet> <directory>bin</directory> </fileSet>
		<fileSet>
			<directory>etc</directory>
			<excludes>
				<exclude>**/.placeholder</exclude>
			</excludes>
		</fileSet>

		<!-- Include the sample directory and all required files -->
		<fileSet> <directory>sample</directory> </fileSet>
		<!-- copy the tests JAR from lsc-core to use to launch OpenDS -->
		<fileSet>
			<directory>target</directory>
			<includes> <include>lsc-core-1.2-SNAPSHOT-tests.jar</include> </includes>
			<outputDirectory>sample/lib</outputDirectory>
		</fileSet>
		<fileSet>
			<directory>src/test/java/org/lsc/opends/config</directory>
			<outputDirectory>sample/etc/config</outputDirectory>
		</fileSet>
		<fileSet>
			<directory>src/test/resources</directory>
			<includes> <include>logback-opends.xml</include> </includes>
			<outputDirectory>sample/etc</outputDirectory>
		</fileSet>
	</fileSets>

	<!-- Include all runtime dependencies of the project -->
	<dependencySets>
		<dependencySet>
			<scope>runtime</scope>
			<outputDirectory>lib</outputDirectory>
			<excludes>
				<exclude>junit:junit</exclude>
				<exclude>org.opends:opends-core</exclude>
				<exclude>hsqldb:hsqldb</exclude>
			</excludes>
		</dependencySet>
		<dependencySet>
			<outputDirectory>sample/lib</outputDirectory>
			<includes>
				<include>org.opends:opends-core</include>
				<include>hsqldb:hsqldb</include>
				<!-- see also the tests jar included in a fileSet above -->
			</includes>
		</dependencySet>
	</dependencySets>
</assembly>
